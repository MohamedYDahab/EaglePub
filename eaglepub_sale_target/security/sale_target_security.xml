<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Salespeople can only see their own targets -->
        <record id="sale_target_rule_salesman" model="ir.rule">
            <field name="name">Sales Target: Salesperson Own Targets</field>
            <field name="model_id" ref="model_sale_target"/>
            <field name="domain_force">[('salesperson_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Sales Managers can see all targets -->
        <record id="sale_target_rule_manager" model="ir.rule">
            <field name="name">Sales Target: Manager See All</field>
            <field name="model_id" ref="model_sale_target"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
